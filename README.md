# c-dbase

Lightweight in-memory key-value store written in C99. Uses a hash table with separate chaining and djb2 hashing. No external dependencies — only the C standard library.

## Build

```bash
make
```

## Run

```bash
./c-dbase
```

## Clean

```bash
make clean
```
