# c-dbase

An in-memory key-value store written in C99. Uses a hash map with chaining, dynamic resizing, and memory leak tracking via `memtrack.h`.

## Build

```bash
make
./c-dbase
```

## Clean

```bash
make clean
```
